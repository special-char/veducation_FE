import React from "react";

const Page = () => {
  return (
    <div>
      {/* <Switch>
        <Route element={Component} path={`${pathname}/component`} />
        <Route
          element={<div>new component</div>}
          path={`${pathname}/new-component`}
        />
      </Switch> */}
      children
    </div>
  );
};

export default Page;
